# MongoDB Atlas Configuration
# Get your connection string from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com
# 2. Select your cluster
# 3. Click "Connect" → "Connect your application"
# 4. Copy the connection string
# 5. Replace <password> with your database user password
# Example: mongodb+srv://username:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
mongoServerUrl=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
mongoDbname=research_collaboration_db

# Redis Configuration
redis_ip=127.0.0.1
redis_port=6379

# Clerk Authentication
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key_here

# Environment
NODE_ENV=development

# Production Deployment Variables
# Railway/Production: Set PORT (automatically provided by Railway)
PORT=4000

# CORS Configuration (comma-separated list of allowed origins)
# Local dev: Use "*" or omit (defaults to "*")
# Production: Set to your frontend URL(s)
# Example: https://your-frontend.vercel.app,https://www.yourdomain.com
FRONTEND_ORIGIN=*

# Base URL for file uploads (optional)
# Local dev: Leave empty (auto-detected from request)
# Production: Set to your backend URL (e.g., https://your-backend.railway.app)
BASE_URL=

# Dev-only: Enable auth debug tools (DO NOT set in production)
# ENABLE_DEV_AUTH_TOOLS=true
